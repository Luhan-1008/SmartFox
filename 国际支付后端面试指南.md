## 0. 先说面试策略（你要拿高分，靠这三点）
1. **一致性**（数据不能错）
2. **安全性**（不能被越权、篡改、攻击）
3. **稳定性**（高并发下不能崩）
所以你回答任何问题，尽量都往这三点靠：
- 这个设计如何保证一致性？
- 这个方案如何保证安全性？
- 这个系统在高并发下如何稳定？
### 0.1 小白速记版（先背这个）
如果你紧张，就用这三句人话：
- 一致性：要么都成功，要么都失败，不能只成功一半。
- 安全性：先确认“你有没有权限”，再让你操作。
- 稳定性：高峰期先排队处理，别一下子把系统压垮。
万能开场句：
> 这个问题我会从三点看：数据别算错、权限别放松、高峰别崩。
1. **一致性**（数据不能错）
2. **安全性**（不能被越权、篡改、攻击）
3. **稳定性**（高并发下不能崩）
所以你回答任何问题，尽量都往这三点靠：
- 这个设计如何保证一致性？
- 这个方案如何保证安全性？
- 这个系统在高并发下如何稳定？
---
## 3. 面试官会细问的第二件事：数据库怎么设计（通俗版先说）
先用人话讲：
你可以按这个顺序答：
- **为什么拆成几张表**：为了把“总信息”和“明细信息”分开，查起来更快，改起来更稳。
- **怎么保证不写乱**：关键步骤放在一次事务里，失败就全部撤销。
- **怎么保证高峰不卡**：给常查条件加索引，减少全表扫描。
30秒口语版：
> 我们数据库设计不是为了复杂，而是为了稳定。提交主记录、答案明细、测试结果分开存；关键写入放一个事务里，失败就全回滚；常用查询路径加索引，这样并发上来时也更稳。
## 3.1 你项目的数据建模主线
建议你用“主单 + 明细 + 结果”的思路讲：
- `Submission`：一次提交主记录（谁、提交哪个实验、什么时候）
- `Answer`：答案明细（支持不同题型）
- `TestResult`：每个测试点结果（输入、期望输出、实际输出、是否通过）
这个设计价值：
1. 可审计（能回放每次提交）
2. 可扩展（题型扩展不推翻主结构）
3. 可异步（后续评测可以队列化）
## 3.3 索引怎么设计
我在项目里的做法是：
- 经常按“用户+时间”查提交记录，就给这类字段建索引。
- 经常按“实验+时间”查历史记录，也建索引。
- 需要按提交ID查明细结果，就给 `submission_id` 建索引。
回答模板（可背）：
> 索引不是越多越好，我会按“最常用的查询路径”建索引。建完后用 `EXPLAIN` 看有没有真正走索引，避免看起来做了优化、实际没生效。
原则：按查询路径建索引，不盲目加。
常见索引点：
- 提交列表：`(user_id, created_at)`
- 某实验提交记录：`(experiment_id, created_at)`
- 判题结果查明细：`submission_id`
- 公告/帖子时间排序：`created_at`
面试加分句：
> 我会用 `EXPLAIN` 验证索引是否命中，并关注回表和扫描行数。
## 3.4 事务边界怎么画
在你项目里，“提交一次实验”通常会连着做几件事：
1. 写 `Submission`（总单）
2. 写多个 `Answer`（每题答案）
3. 写 `TestResult`（测试结果）
这些应该放在一个事务里。这样即使中间出错，也不会留下“只写了一半”的脏数据。
提交链路是一个事务：
1. 写 `Submission`
2. 写多个 `Answer`
3. 写 `TestResult`
任一步失败全部回滚，避免“有提交主记录但没有结果明细”。
## 3.5 如果面试官问“支付场景你怎么设计表”（通俗版）
你可以先类比，不要一上来堆术语：
- `PaymentOrder`：支付总单（像订单主记录）
- `PaymentAttempt`：每次尝试记录（比如网络抖动重试）
- `LedgerEntry`：记账明细（每笔钱怎么流转）
- `ReconciliationRecord`：对账记录（和渠道核对是否一致）
一句话回答模板：
> 支付表设计的核心是“主单清晰、每次尝试可追踪、账务明细可回放、对账结果可核对”。
你可以类比回答：
- `PaymentOrder`（支付主单）
- `PaymentAttempt`（每次渠道请求尝试）
- `LedgerEntry`（账务分录）
- `ReconciliationRecord`（对账记录）
并强调：
> 支付核心一定是主单状态机 + 分录不可篡改 + 对账可追溯。
---
## 4. 接口设计会被问很细：怎么答
## 4.1 REST 接口设计原则（你能讲的）
- 资源化命名：`/experiments`、`/submissions`、`/notices`
- 方法语义：`GET` 查、`POST` 新增、`PUT/PATCH` 修改、`DELETE` 删除
- 状态码规范：`200/201/400/401/403/404/500`
- 统一返回结构：`code/message/data`（便于前端和监控）
## 4.2 参数校验怎么做
你要说：
- 入参必填校验（如题目ID、代码文本）
- 类型与范围校验（如文件大小、分页参数）
- 业务规则校验（如角色是否允许发布）
## 4.3 错误处理怎么做
不要只说“try/except”。说完整：
- 业务错误（参数不合法、权限不足）返回可读错误码；
- 系统错误统一兜底，避免内部堆栈泄露；
- 关键异常写日志，便于排障。
## 4.4 面试官追问：“如何版本化 API？”
回答：
- URL 版本（如 `/api/v1/...`）或 Header 版本；
- 新增字段尽量向后兼容；
- 废弃接口有迁移窗口，不硬切。
---
## 5. 认证与权限（JWT + RBAC）怎么讲更像支付后端
1. 为什么 access token 要短期？
   - 减少泄露后可用窗口。
2. refresh token 有什么风险？
   - 一旦泄露可持续换新 token，所以需黑名单/轮换。
## 5.3 你要主动承认的改进
## 6. Docker 沙箱怎么实现、如何回答“安全吗”
实现流程
1. 用户提交代码与题目配置（测试用例、内存限制、超时）
2. 启动隔离容器运行代码
3. 喂入测试输入，获取输出
4. 对比期望输出并记录结果
5. 删除容器，回收资源
## 6.2 已做的安全基线
- 容器隔离执行环境
- 内存限制
- 网络禁用或受限
- 执行后强制清理容器
## 6.3 面试官再追问“还不够，怎么办？”
你回答这四条：
- CPU 限额与进程数限制
- 非 root 用户运行容器
- 只读文件系统与临时目录隔离
- seccomp/apparmor 限制系统调用
再补一句：
当前是教学场景基线安全，若用于支付级场景会按最小权限和多层隔离继续加强。
---
## 7.3 异步化怎么讲清楚
## 8. 可用性与稳定性：你要会说“故障怎么扛住”
## 8.1 常见故障场景
- 下游服务变慢
- 数据库瞬时高负载
- 突发流量高峰
- 某版本发布后出现异常
## 8.2 对应策略
- **限流**：先挡住超额请求
- **熔断**：下游挂了就快速失败，不拖垮全局
- **降级**：保核心链路，非核心功能暂时简化
- **回滚**：版本异常时快速回退
## 8.3 K8s 你至少要会说的五个词
- Deployment（部署与副本管理）
- Service（服务发现与负载均衡）
- Ingress（入口路由）
- HPA（自动扩缩容）
- Rollout/Rollback（滚动发布与回滚）
---
## 9. 面试最爱追问：幂等、重复提交、一致性
## 9.2 你项目哪里存在幂等风险
提交和判题接口，如果客户端因超时重试，可能产生重复提交记录。
## 9.3 怎么改（标准答案）
1. 请求带 `idempotency_key`
2. 数据库加唯一约束（如 `user_id + experiment_id + idempotency_key`）
3. 首次成功后缓存/落库结果
4. 重复请求直接返回首次结果
## 9.4 支付映射
支付回调、扣款请求、退款请求都必须幂等，否则就会重复扣款/重复退款。
---
## 10. 面试官会“卡你”的风险点与应对
## 10.1 风险点列表
1. 开发态安全配置（`DEBUG=True`、宽松 CORS 等）
2. 部分关键接口权限放开
3. 并发更新丢失（`+=1` 类更新）
4. N+1 查询
5. 幂等缺失
6. 沙箱隔离仍有边界风险
## 10.2 标准应对话术（建议背）
> 这个问题我们在教学场景里优先保证功能闭环，确实还有生产级改进空间。我的改进优先级是：
> 1）先收敛安全基线（权限、密钥、配置）；
> 2）再补一致性（幂等键、唯一约束、并发控制）；
> 3）最后补可观测性和容量治理（日志、指标、告警、压测回归）。
---
## 11. 你必须准备的“细节题”与可背答案
## 11.2 数据库为什么这样设计？
答：
> 我采用“主记录+明细+结果”三层结构。`Submission` 记录一次提交，`Answer` 记录每题答案，`TestResult` 记录逐用例判题结果。优势是可审计、可扩展、可异步，避免单表过大和字段混乱。
## 11.3 事务怎么用，边界怎么选？
答：
> 多表写入是一个原子操作，必须放在同一事务里。提交主记录、答案明细、测试结果任一步失败就回滚，避免半成功状态。事务边界尽量只包必要写操作，减少锁持有时间。
## 11.4 索引怎么做？
> 先看查询路径，再建索引。例如用户提交列表按用户和时间查询，我会建 `(user_id, created_at)`。建完后用 EXPLAIN 验证，避免无效索引。高频更新字段不会盲目加索引，避免写放大。
## 11.5 怎么处理高并发下的更新冲突？
> 对计数类更新用原子更新，必要时加乐观锁或行锁，避免读改写竞态。资金敏感链路会使用更严格锁策略并缩小事务范围。
## 11.6 沙箱为什么选 Docker？
> Docker 隔离成本低、生态成熟、启动快，适合教学场景代码执行。我们结合内存限制和禁网策略降低风险。生产场景可进一步加强 CPU/进程限制和系统调用隔离。
## 11.7 CI/CD 到底提升了什么？
> 核心是把“构建、测试、部署”流程标准化和自动化。提交后自动执行测试与构建，减少人工漏测和环境不一致问题，提高发布成功率和迭代速度。
## 11.8 压测结果如何可信？
> 我会明确压测环境、并发模型、指标口径，并区分“接口响应时间”和“任务完成时间”。每次优化后做回归压测，确保收益真实可复现。
---
## 12. 国际支付方向的“迁移表达”
你可以把项目迁移成支付语言（背这段）：
> 虽然项目是教学平台，但核心工程思想和支付后端一致：
> - 一致性：用事务保证多表写入一致，下一步补幂等和唯一约束；
> - 安全性：用 JWT + RBAC 做身份和权限控制，后续加强密钥管理和审计；
> - 稳定性：通过自动化测试、压测和容器化部署提升可用性，后续补齐限流熔断与可观测性。
---
## 13. 小白术语翻译表（面试现场忘词可救命）
- 架构：系统怎么分工
- 事务：一组操作要么都成功要么都失败
- 幂等：重复请求不重复生效
- 索引：数据库目录
- 并发：很多人同时访问
- 限流：控制进入系统的流量
- 熔断：下游坏了先断开，防止拖垮自己
- 降级：先保核心功能
- RBAC：按角色分权限
- 可观测性：靠日志和指标看清系统状态
---
## 14. 面试前一天复习清单（执行版）
1. 背熟 1 分钟自我介绍 + 3 分钟项目深挖
2. 能白话解释：事务、幂等、索引、限流、熔断
3. 准备 2 个“做成了”的故事 + 1 个“复盘改进”故事
4. 准备 1 套“业务边界划分”答案
5. 准备 1 套“数据库设计与索引”答案
6. 准备 1 套“沙箱安全”答案
7. 准备 1 套“高并发优化”答案
8. 准备 3 个高质量反问
---
## 15. 面试官反问（你可以直接问）
1. 团队当前国际支付里最难的技术挑战是高可用、分布式一致性还是风控时效？
2. 实习生通常优先参与哪些链路（支付、对账、清结算、风控）？
3. 团队在可观测性、故障演练、容量规划方面的实践是怎样的？
4. 团队对实习转正最看重的 2~3 项能力是什么？
---
## 16. 最后收尾金句（建议背）
> 我在这个项目里不仅完成了功能开发，更有意识地从一致性、安全性、稳定性三个维度做工程化落地和复盘。对我来说，国际支付后端的吸引力在于把这些能力提升到更严格的生产级标准。
---
## 24. 数据库部分（通俗版重梳理，面试优先用这版）
你可以把数据库理解成“开店记账本”：
- `Submission`：总单（这次是谁、提交了什么）
- `Answer`：明细（每道题的答案）
- `TestResult`：质检记录（每个测试点过没过）
先背这句开场：
> 我把数据库设计成“总单 + 明细 + 质检记录”，因为这样查询清楚、扩展方便、也不容易把数据写乱。
### 24.1 为什么拆成这三层（你要讲依据，不要只讲概念）
1. 列表页通常只查总单，不需要每次带上全部测试点。  
2. 测试结果增长最快（一次提交会产生多条测试点记录），单独拆表更利于 MySQL 查询性能。  
3. 出错时可以整条链路回滚，避免“有提交、没结果”的半成品数据。
### 24.2 你们“拆分数据库”到底是什么意思（面试很爱抠）
你要准确说：
> 目前是 MySQL 单库（`lab_platform`），没有做物理分库分表。  
> 我们做的是逻辑拆分：按业务模块拆表，按读写路径拆主表和明细表。
举例：
- 评测域：`Experiment / Submission / Answer / TestResult`
- 论坛域：`Question / Comment / Like`
- 公告域：`Notice / NoticeReaders`
- 资料域：`LearningMaterial`
### 24.3 索引怎么讲（人话）
> 索引就是数据库目录，按“最常查什么”来建，不是越多越好。
你可以说：
- 按用户查提交历史：`user_id + submitted_at`
- 按实验查某学生提交：`experiment_id + user_id`
- 查一次提交的结果明细：`submission_id`
- 公告/帖子按时间倒序：`created_at`
加分句：
> 我会用 `EXPLAIN` 看 SQL 有没有走索引，避免全表扫描。
### 24.4 事务怎么讲（贴你项目）
> 提交实验时，我把“写总单、写答案明细、写测试结果”放在一个事务里。任何一步失败都撤销。  
> 这样不会出现“有记录但不完整”的数据。
### 24.5 重复提交怎么讲（支付思路）
> 网络卡的时候用户会重复点提交，所以要保证“同一件事只算一次”。  
> 做法是请求带唯一号（幂等键）+ MySQL 唯一约束，重复请求直接返回第一次结果。
### 24.6 外键策略怎么讲
- 强依赖关系（答案依赖提交）：父删子删（级联）
- 历史有价值关系（公告/资料）：关联对象删了，记录保留（置空）
### 24.7 20 秒总结（可直接背）
> 我们现在是 MySQL 单库，但做了清晰的逻辑拆分：按业务模块拆表，按读写路径拆总单和明细。  
> 核心链路用事务保证一致性，索引按查询路径设计，重复提交用唯一约束兜底。  
> 这套思路可以平滑迁移到支付场景。
---
## 17. “体现你理解”的回答模板（从做了什么到为什么这么做）
很多同学只会说“我做了 A/B/C”。
你要升级成：**背景 → 方案 → 取舍 → 风险 → 改进**。
统一模板：
> 当时我们的业务背景是（xxx），约束是（时间/人力/环境）。
> 我选择了（方案），因为它在（稳定性/开发效率/可维护性）上更平衡。
> 这个方案的代价是（xxx），所以我补了（xxx）。
> 现在复盘看，如果要上生产级，我会优先改（xxx）。
### 17.1 业务边界划分（高质量版）
可以这样说：
> 我不是按“页面”拆模块，而是按“能力边界”拆模块。比如“评测域”只负责提交、判题、结果，不负责用户登录；“用户域”只负责身份和权限，不承载具体业务。
> 这样设计的好处是：
> 1）职责单一，改动影响面小；
> 2）权限收口清晰，安全策略集中；
> 3）未来服务化拆分时迁移成本低。
再补一句取舍：
> 代价是前期抽象成本更高，需要统一接口契约和错误码规范，但长期维护收益更大。
### 17.2 数据库设计（高质量版）
可以这样说：
> 我采用了“主记录 + 明细 + 结果”的分层建模，而不是把所有信息塞到一张大表。核心原因是查询模式不同：
> - 列表页关心主记录；
> - 回放页关心明细；
> - 判题分析关心测试结果。
> 分表后每类查询都更清晰，索引也更好设计。
再补一句风险意识：
> 分层建模会增加事务复杂度，所以我把提交链路放在事务里，保证主记录、答案和结果的一致性。
### 17.3 Docker 沙箱（高质量版）
可以这样说：
> 我选 Docker 的原因不是“流行”，而是它在“隔离性、可复制环境、启动速度”上比较平衡。教学场景里既要安全，也要控制资源成本，容器是现实可行的方案。
再补一句边界：
> 我也清楚容器不是绝对安全，所以后续会加强非 root、seccomp、只读文件系统和 CPU 配额。
### 17.4 压测与性能（高质量版）
可以这样说：
> 我把压测分成两类指标：
> - 用户感知指标：接口 P95/P99；
> - 系统指标：QPS、错误率、CPU/内存。
> 优化时遵循“先定位瓶颈再优化”的原则，避免拍脑袋调参。
---
## 18. 业务边界与数据库设计（可直接画图讲）
> 这部分是你说“面试会问很细”的核心，我给你可以口述的版本。
### 18.2 数据库核心实体（口述）
- `user`：用户、角色
- `experiment`：实验主体
- `question_*`：题型实体（选择/填空/编程）
- `submission`：一次提交主单
- `answer`：每题答案明细
- `test_result`：逐测试点结果
- `notice` / `post` / `comment`：内容互动
- `resource`：资料与文件路径
### 18.3 关键查询与索引（口述）
1. 查“某学生最近提交”
   - SQL 过滤：`user_id + created_at`
   - 索引：`(user_id, created_at)`
2. 查“某实验所有提交”
   - SQL 过滤：`experiment_id + created_at`
   - 索引：`(experiment_id, created_at)`
3. 查“某次提交的测试点结果”
   - SQL 过滤：`submission_id`
   - 索引：`submission_id`
4. 公告按时间倒序
   - 索引：`created_at`
### 18.4 一致性边界（口述）
同一事务内：
- `submission` 创建
- `answer` 批量写入
- `test_result` 写入
事务外（可异步）：
- 通知推送
- 非核心统计更新
原因：
- 把核心一致性留在事务内；
- 把耗时非核心逻辑移出事务，减少锁持有时间。
---
## 19. 可能还会问到的问题（扩展题库，含答题方向）
### 19.1 架构与工程
1. **为什么不用 Spring Boot / Go，而用 Django？**  
答题方向：团队熟悉度、开发效率、交付周期、生态支持；并承认高并发场景可逐步服务化。
2. **你们为什么现在不彻底微服务化？**  
答题方向：当前规模下单体+模块化成本更低；先把边界和契约打稳，再拆服务。
3. **CI/CD 流水线失败时怎么处理？**  
答题方向：测试失败阻断部署；保留失败日志；回滚到上个稳定镜像。
4. **灰度发布怎么做？**  
答题方向：小流量先行、监控指标观测、异常自动回滚。
### 19.2 数据库与一致性
5. **为什么 MySQL 不用 MongoDB？**  
答题方向：关系清晰、事务能力强、强一致需求更匹配。
6. **隔离级别会造成什么问题？**  
答题方向：脏读/不可重复读/幻读；资金链路通常需要更强控制并配合行锁。
7. **死锁怎么排查？**  
答题方向：查看死锁日志、统一加锁顺序、缩小事务范围、减少长事务。
8. **如何做数据归档？**  
答题方向：冷热分层、按时间分区、历史表归档，保障在线库性能。
### 19.3 接口与安全
9. **如何防止接口被刷？**  
答题方向：限流、验证码、IP/设备风控、行为审计。
10. **如果 token 泄露怎么办？**  
答题方向：短效 access、refresh 轮换、黑名单、异常登录告警。
11. **上传文件怎么防攻击？**  
答题方向：类型/大小校验、重命名、防目录穿越、病毒扫描、对象存储隔离。
12. **如何防越权？**  
答题方向：不仅校验角色，还校验数据归属（对象级权限）。
### 19.4 并发与性能
13. **异步任务堆积了怎么办？**  
答题方向：多 worker、队列分级、重试退避、死信队列、告警。
14. **缓存和数据库不一致怎么办？**  
答题方向：Cache Aside、删除缓存优先、短 TTL、热点 key 保护。
15. **如何避免重复消费任务？**  
答题方向：消费幂等、去重表/唯一键、状态机防重入。
16. **如何定义系统容量上限？**  
答题方向：基于压测得出拐点（P99恶化/错误率上升），据此做容量规划。
### 19.5 支付方向迁移（高价值）
17. **如果让你设计支付下单接口，你最先考虑什么？**  
答题方向：幂等、签名验签、状态机、超时重试、审计日志。
18. **对账为什么重要？**  
答题方向：内外部账务对齐，发现漏单/重单/金额不一致，保障资金安全。
19. **清结算和支付下单有什么差别？**  
答题方向：下单偏实时交易，清结算偏批处理和资金归集。
20. **资金系统为什么强调“不可篡改流水”？**  
答题方向：可追溯、可审计、合规要求。
---
## 20. 最终冲刺：把“我会做”升级成“我理解为什么”
你最后要达到这个表达层次：
- 我会做接口，不如我知道接口为什么要有幂等；
- 我会建表，不如我知道为什么要主单/明细分层；
- 我会用 Docker，不如我知道它的安全边界；
- 我会做压测，不如我知道如何用数据驱动优化。
你可以用这段收尾：
> 这个项目让我从“功能实现”走到“工程化思维”。我不只关注能不能跑通，还会关注一致性、安全和稳定性。在国际支付场景里，我会把这种思维进一步落到幂等、账务分录、对账和高可用治理上。
> 我在这个项目里不仅完成了功能开发，更有意识地从一致性、安全性、稳定性三个维度做工程化落地和复盘。对我来说，国际支付后端的吸引力在于把这些能力提升到更严格的生产级标准。
---
## 21. 基于当前代码的 ER 讲解稿（MySQL 细节版）
> 这一节用于你在面试中“照着讲”，强调你们是**单库 MySQL（lab_platform）**下的逻辑拆分，而不是物理分库分表。
### 21.1 先讲清“拆分”的真实含义
可直接说：
> 我们当前是 MySQL 单库架构，没有做数据库中间件路由或跨库分片。我们做的是“按业务域拆表 + 按写入链路拆主从表 + 按题型拆表”。
- 按业务域：用户域、实验评测域、论坛域、公告域、资料域
- 按写入链路：`submission`（主）→ `answer`（明细）→ `test_result`（结果明细）
- 按题型差异：`choice_problem` / `fill_problem` / `coding_problem`
---
### 21.2 核心实体关系（口述 ER）
#### 用户域
- `user`（自定义用户）
  - 关键唯一约束：`username`、`email`、`student_id`
  - 角色字段用于 RBAC
#### 实验与评测域（主链路）
- `experiment`
- `choice_problem` / `fill_problem` / `coding_problem`（均外键到 `experiment`）
- `submission`（外键：`experiment_id`, `user_id`）
- `answer`（外键：`submission_id`, `content_type_id`, `object_id` 形成 GFK）
- `test_result`（外键：`answer_id`）
- `coding_submission`（外键：`coding_problem_id`, `user_id`）
- `experiment_students`（M2M 中间表）
#### 论坛域
- `forum_question`（外键：`author_id`）
- `forum_comment`（外键：`question_id`, `author_id`）
- `forum_question_likes`（M2M 中间表）
#### 公告域
- `notice`（外键：`publisher_id`, 可选 `experiment_id`）
- `notice_readers`（M2M 中间表，记录已读关系）
#### 资料域
- `learning_material`（外键：`uploader_id`，可选 `question_set_id`）
---
### 21.3 为什么这样拆（面试官最关心）
#### 依据 1：查询路径不同
- 列表页主要查 `submission` 头信息（谁、哪次、状态）
- 详情页才查 `answer` / `test_result`
- 把高频轻查询和低频重查询分开，减少无效 IO
#### 依据 2：数据增长速度不同
- `test_result` 按“提交次数 × 测试点数”增长最快
- 独立表可单独优化索引、归档策略，不拖慢主表
#### 依据 3：字段稀疏差异明显
- 编程题特有 `test_cases` / `timeout` / `mem_limit`
- 若强塞单表，会有大量 NULL 列 + 条件分支复杂化
#### 依据 4：一致性边界明确
- `submission + answer + test_result` 在同一事务内
- 通知、统计等非核心逻辑移出事务，减少锁持有时长
---
### 21.4 MySQL 约束与外键策略（细节）
#### 强从属关系用 `ON DELETE CASCADE`
- 例如：实验删除 → 对应题目删除；提交删除 → 对应答案/结果删除
- 理由：子记录失去语义，应自动清理，避免孤儿数据
#### 弱引用关系用 `SET NULL`
- 例如某些资料/公告保留历史，即使引用对象删除仍需保留记录
- 理由：兼顾历史可追溯与数据完整性
#### 唯一约束
- 用户关键字段唯一，防止身份冲突
- 可扩展：提交链路引入 `(user_id, experiment_id, idempotency_key)` 唯一约束做防重
---
### 21.5 索引设计（结合你项目）
当前你已具备的典型索引思路：
- `coding_submission(user_id)`：查某用户提交历史
- `coding_submission(created_at)`：时间排序
- `coding_submission(coding_problem_id, user_id)`：按题目+用户过滤
- `submission(user_id)`：用户提交列表
- `submission(submitted_at)`：时间维度分页
- `submission(experiment_id, user_id)`：按实验+用户定位
可主动补充优化（加分）：
- `answer(content_type_id, object_id)` 联合索引，优化 GFK 反查
- 高频分页场景可补复合索引 `(user_id, submitted_at)` 避免 filesort
- 索引上线前后用 `EXPLAIN` 比较扫描行数与回表情况
---
### 21.6 你可以直接背的 40 秒数据库回答
> 我们当前是 MySQL 单库，不是物理分库分表。拆分主要体现在三层：按业务域拆表、按提交链路拆主明细、按题型拆题库表。评测链路里 `submission` 是头，`answer` 是题目级明细，`test_result` 是测试点明细，这样能兼顾审计回放、扩展性和查询性能。MySQL 层面我们依靠 InnoDB 事务保证提交链路一致性，用外键级联/置空区分强弱关系，并按查询路径建立联合索引，后续流量增长再做按域物理分库演进。
---
## 22. 数据库细节追问 10 题（高频 + 标准答法）
### Q1：你们到底做了“分库分表”吗？
**答法：**没有做物理分库分表；当前是单库 MySQL。我们先做逻辑拆分（按域/按链路/按题型），因为当前阶段收益最高、复杂度最低。后续再按流量做物理拆分。
### Q2：为什么 `submission`、`answer`、`test_result` 要分三张？
**答法：**职责和查询模式不同。头信息高频查询，明细和结果低频下钻；分层后索引更精准，且 `test_result` 增长快可单独治理。
### Q3：为什么不用一张 `question` 大表存所有题型？
**答法：**不同题型字段差异大，强行合并会有大量空列和复杂条件；分表后结构更清晰，约束更自然，查询也更可控。
### Q4：GFK（`content_type + object_id`）有什么利弊？
**答法：**优点是统一答案模型、扩展题型快；缺点是数据库层无法做强外键完整性，联查复杂。当前教学场景接受该取舍，并通过索引和业务校验兜底。
### Q5：提交链路事务为什么不把“通知推送”也放进去？
**答法：**通知属于非核心副作用，放入事务会拉长锁时间。我们把核心一致性（主记录+明细+结果）放事务内，通知异步处理，兼顾一致性与吞吐。
### Q6：你们怎么防重复提交？
**答法：**当前版本存在重试重复写风险。生产改法是引入 `idempotency_key`，并在 MySQL 建唯一索引 `(user_id, experiment_id, idempotency_key)`，重复请求直接返回首次结果。
### Q7：哪些字段最值得建联合索引？为什么？
**答法：**按实际过滤组合建：如 `(experiment_id, user_id)`、`(user_id, submitted_at)`。这类查询最频繁，能减少全表扫描与回表成本。索引是否有效用 `EXPLAIN` 验证。
### Q8：MySQL 隔离级别和锁你怎么考虑？
**答法：**默认 RR 可满足大多数场景。核心一致性写入走事务，必要时用 `SELECT ... FOR UPDATE` 控制并发修改；同时缩小事务范围降低锁冲突。
### Q9：如果 `test_result` 表越来越大怎么办？
**答法：**先做冷热分层与归档（按时间/学期归档），保留在线库近期开销；再视规模做分区或按域分库，避免影响在线查询。
### Q10：为什么国际支付会更强调数据库设计细节？
**答法：**支付对“资金正确性+可审计性”要求极高。表结构必须支持幂等、防重、状态机流转、对账追溯，任何字段和约束设计都会直接影响资金安全。
---
## 23. 可直接放在白板上的“表关系简图（文字版）”
```text
user
 ├─< experiment_students >─ experiment
 │                          ├─< choice_problem
 │                          ├─< fill_problem
 │                          └─< coding_problem ─< coding_submission >─ user
 │
 └─< submission >─ experiment
       └─< answer (content_type_id, object_id)
             └─< test_result
user ─< forum_question ─< forum_comment
forum_question ─< forum_question_likes >─ user
user ─< notice
notice ─< notice_readers >─ user
user ─< learning_material >─ (optional) experiment
```
你讲图时只强调三点：
1. 评测主链路是 `submission -> answer -> test_result`
2. 内容域与评测域解耦
3. 用户域是横切能力，所有域都引用但不反向耦合
你最后要达到这个表达层次：
- 我会做接口，不如我知道接口为什么要有幂等；
- 我会建表，不如我知道为什么要主单/明细分层；
- 我会用 Docker，不如我知道它的安全边界；
- 我会做压测，不如我知道如何用数据驱动优化。
你可以用这段收尾：
> 这个项目让我从“功能实现”走到“工程化思维”。我不只关注能不能跑通，还会关注一致性、安全和稳定性。在国际支付场景里，我会把这种思维进一步落到幂等、账务分录、对账和高可用治理上。
> 我在这个项目里不仅完成了功能开发，更有意识地从一致性、安全性、稳定性三个维度做工程化落地和复盘。对我来说，国际支付后端的吸引力在于把这些能力提升到更严格的生产级标准。
